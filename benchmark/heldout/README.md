# Held-out boundary

Real hidden task material belongs outside the repository. Store only hashes and loader
contracts here. The smoke benchmark has no task labels and exists only to verify the
experiment lifecycle.
